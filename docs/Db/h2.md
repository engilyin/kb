# H2

## run script

To run the `init-h2.sql` script when starting the H2 database, include the following in your JDBC URL:

```
     "jdbc:h2:mem:njcitation;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;INIT=RUNSCRIPT FROM 'classpath:init-h2.sql'"
```